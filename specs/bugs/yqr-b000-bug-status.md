# Bug Status Tracker

Single source of truth for the state of every `yqr-bNNN` bug. Update this file
in the same change that opens, advances, or resolves a bug (mirrors the feature
status tracker convention).

**Status legend:** Open · In Progress · Fixed · Resolved · Won't Fix · Duplicate

## Open

| Bug | Title | Severity | Status | Related |
|-----|-------|----------|--------|---------|
| [b022](yqr-b022-colon-at-end-of-input-is-not-a-mapping.md) | A file with no trailing newline after a blank value is misread, and `validate` calls it invalid | Medium | Open — **fixed upstream, awaiting a release** (noyalib#312, 2026-08-23). A `:` at **end of input** is not read as a mapping value indicator, though a `:` followed by a space or a line break is — so `a:` and `a:\n`, the same document one byte apart, load as a string and a mapping respectively. Three faces in yqr: `.a` over `a:` errors with *"cannot index string"*; `a: 1\nb:` fails to parse outright; and **`validate` reports `Y001` on valid YAML**, which is the worst of them, since that command's whole job is to answer whether a file is correct. PyYAML and Psych agree with each other on all nine shapes measured and with noyalib on the five controls. Not a fidelity defect — the identity read stays byte-exact, so `a001` §1 holds; what is wrong is the value. **Deliberately not worked around**: appending a newline before parsing would change the bytes the fidelity engine slices, trading a wrong value for a broken guarantee. Found while patching `b021` upstream, by a test case that could not be written because the input never became a mapping | `yqr-b021`, `yqr-f012`, `yqr-a001` |
| [b021](yqr-b021-cannot-write-a-value-into-an-implicit-null.md) | A value cannot be written into an implicit null, and the refusal says the path does not exist | Low | Open — **fixed upstream, awaiting a release** (noyalib#310 / noyalib#311, 2026-08-23). `a:` with no value reads as `null` and cannot be written: `.a = 1` answers *"path not found: a"* for a path the line above just printed. Writing `null` there **is** accepted, because the `b018` guard skips it — so the operation succeeds exactly when it does nothing. Two faults, and only one is the wording: unlike a merged-in key or an alias site there is a real entry here, with a key token of its own, and `a:` -> `a: 1` restructures nothing. Filling in a value someone left blank is ordinary work on a config file, so the **refusal** is the questionable half. Cause is upstream's `resolve_span` reporting a zero-width leaf as `None`, which `write_span` renders as `path not found` — the same undifferentiated answer `b020` §6.1 measured, from a different shape. Found by a test written to hold `b020`'s check *off* a case that is not a merge, and that test pins the boundary. The fix needed an insertion point rather than a span to replace, and both open questions — the empty `-` item and the `a:   # todo` trailing comment — were settled by the same mechanism: noyalib#165's zero-width leaf already carries the indicator's position, so `resolve_span` was discarding one level too early. `span_at` discards it now (keeping #165's read contract) and `write_span` turns it into an insertion point one byte past the `:` / `-`, which puts the value ahead of a trailing comment by construction and makes the `-` case the same rule. The measurement also turned up a silent duplicate-key defect yqr cannot reach, and **noyalib#312**, filed as `b022`. Verified downstream with a local `patch.crates-io`. On adoption, `a_mappings_own_entry_is_untouched_by_the_merged_key_check` flips — it pinned this refusal on purpose | `yqr-b020`, `yqr-b019`, `yqr-f006`, `yqr-b022` |

## Resolved

| Bug | Title | Severity | Status | Related |
|-----|-------|----------|--------|---------|
| [b023](yqr-b023-version-prints-an-empty-commit-hash.md) | `--version` prints an empty commit hash when yqr is installed from crates.io | Low | Resolved **2026-08-23**, found by smoke-testing the published `0.7.0` binary rather than a local build. Every yqr installed from crates.io reported `yqr 0.7.0 (, built ...)` — an empty pair of parentheses where the commit goes. `build.rs` asked `git` and never checked `status.success()`: outside a checkout `git rev-parse` exits **128** with its complaint on stderr and an empty stdout, so `output()` is `Ok`, the string is `""`, and the `"unknown"` fallback — reachable only if the git binary is missing outright — never fired. The one case it was written for is the one it did not catch. `"unknown"` was the wrong target anyway: Cargo writes the commit into `.cargo_vcs_info.json` when it packages a crate, so the answer was on disk the whole time. Two routes now, `git` then the packaged manifest, with `"unknown"` left for a tree that is neither and now genuinely reachable; all three verified by building from a checkout, from the unpacked `0.7.0` tarball, and from that tarball with the manifest deleted. The identical construct sits in **accentcms**'s `build.rs`, unexercised only because it is not published — worth porting before its first release rather than after | `yqr-m001`, `yqr-m004` |
| [b020](yqr-b020-write-says-path-not-found-for-a-path-it-can-read.md) | A write to a merge-produced entry reports `path not found` for a path yqr just read | Low | Resolved **2026-08-22**, filed and fixed the day `b019` landed. `yqr '.c.k'` printed `1`; `yqr '.c.k = 9'` on the same file answered *"YAML parse error: path not found: c.k"* — the refusal correct, the reason contradicting the tool's own read. §4 preferred fixing it upstream and asked for a measurement first; the measurement moved the decision. **Upstream cannot re-word it**: `resolve_span`, the single resolver behind `span_at` and `write_span`, returns a bare `None` for a merge-produced key, the same answer as for a key that is not there, so option 2 needs a third outcome in the resolver rather than a better sentence. **But upstream already words it correctly in `rename_key`** — its own two mutators answer the same question about the same bytes two ways, the `b012` shape, still worth filing. And the **two-voices objection turned out to be to a wider fix than the one needed**: `value_is_borrowed` rests on two independent facts, and the key one (`key_span` is `None` on a key-terminated path) is exact and complete, so taking the message over for that arm alone gives each fact one voice. The alias arm still falls through to upstream's wording, which was already good. `Borrowed` is an enum rather than a bool to keep the arms apart, with a unit test on both. Verified as a message change and nothing else: 96 old-vs-new comparisons across seven documents × `=` and `|=` × matching and differing values gave zero differences in exit code or stdout and 16 in the message, all of them the merged-key cases. The fidelity guide now quotes the refusal it had to paraphrase a commit earlier. Review of the fix found the first wording named a remedy yqr declines — writing the override *is* this check, and inserting a sibling is refused too unless the mapping already owns one — so it names the anchor route only, with a test that the route works rather than that the sentence exists (`yqr-f025`). That review also closed `b019`'s sequence-item gap and turned up `b021` | `yqr-b019`, `yqr-b012`, `yqr-b021`, `yqr-f025`, `yqr-a002` |
| [b019](yqr-b019-no-op-guard-swallows-a-refusal-and-skips-comments.md) | The no-op guard runs ahead of the writer's refusals, and does not cover comments | Medium | Resolved **2026-08-22**, in review of `b018` the same day — two defects, one cause. The `b018` guard skipped the write when the new value matched the old, and it ran **before** the writer, so every refusal that does not depend on the value written became a silent exit 0: `.b = 2` over `b: *x` refuses, `.b = 1` returned 0 with the alias still there and nothing said, and a later edit to the anchor would move `b` with it. Same for a path through a `<<` merge. `b018` §7.2 had already recorded one refusal the guard was *allowed* to skip, which is what made this look intended; the line between them is now stated — the guard may skip past yqr's own **expressive** limits, where the document already holds what was asked for, but not past a property of the **document**, where writing would have done real work. Second face: the rule stopped at values, so `line_comment(.a) = "tight"` over `a: 1 #tight` re-spelled the line to `# tight` — `yqr-a001` §1 again, and it broke the read/write pair. Fixed by establishing whether the value is *borrowed* before skipping and falling **through** to the writer on a borrowed site, so the diagnostic stays upstream's rather than becoming a second copy to drift; noyalib decides the same question in a private `write_span`, so yqr establishes it from the public span API. A cloned-document probe was measured and rejected: it refuses `.a = 1` on `a: &x 1`, a genuine `b018` no-op, and a false refusal on a no-op is what `b018` exists to prevent. Six CLI tests, one unit test on the predicate and two corpus cases, both directions pinned. The one shape the check could not reach — an alias-valued **sequence item**, which has no key to measure against — was recorded as a known gap and **closed 2026-08-22** in `b020`'s review, which called it what it was: a live false success, where the write succeeds exactly when it would have been refused. Both rules turned out to be one question with two floors (a key, or the item ahead of it), plus an ancestor walk for an item of a sequence that is itself borrowed | `yqr-b018`, `yqr-a001`, `yqr-a002`, `yqr-f006`, `yqr-f007`, `yqr-f008` |
| [b018](yqr-b018-idempotent-assignment-recanonicalises-a-scalar.md) | `.n = .n` rewrites `0640` as `640`: an assignment that changes nothing still re-spells the scalar | Medium | Resolved — the typed model cannot carry a number's spelling, so `set_value` re-emitting the value it was given canonicalised it: `0640` became `640` and a `1.10` version pin became `1.1`, at exit 0, on the two examples the fidelity guide leads with — and on an assignment that changed nothing, which is `yqr-a001` §1's own counter-example. Quoted strings were unaffected. Found by `yqr-f008`'s first acceptance test, which fixed the `\|=` half inline; the `=` half is fixed **2026-08-22**, and the rule now lives in one place both operators reach (`set_value_unless_unchanged`) rather than as a copy each — the duplication shape yqr had filed against accentcms as `b190` §1 the day before. `AssignTarget::Existing` carries `{ path, current }` now, since the guard needs to know what is there. One behaviour change outside the bug's statement, found by an existing test and pinned on both sides: a no-op at an **unaddressable** key now succeeds rather than reporting the `yqr-f007` §6 limit, because no write was attempted. Six tests hold the fix — three CLI, one unit, two corpus; of the five spellings the CLI test walks, three reproduce with the guard removed and two (`"30"`, `i64::MAX`) are controls. Review of the fix found the guard was placed too **early**, swallowing the refusals that do not depend on the value written, and that the rule stopped short of comments; both corrected in `b019` | `yqr-a001`, `yqr-f006`, `yqr-f008`, `yqr-b001`, `yqr-b019` |
| [b017](yqr-b017-accent-llms-txt-generation-gaps.md) | accent's `llms.txt` generation: one defect, two gaps, two non-defects | Low | Resolved — a review of yqr's published `llms.txt` proposed five generator changes; read against `accentcms` source, **two were not defects** (an authored entry text already worked, under the key `lead:` rather than `description:`; and the listing cut-off was `max_links_per_section` at its default). The three real ones, plus two more found by *acting* on the report rather than writing it: a section's index page was emitted again as its own first entry, spending a capped slot on a duplicate; sections ordered alphabetically by directory while `menu.order` was ignored; a capped section did not say so; an **authored** entry was cut mid-word at 120 characters, making the advice "author a `lead:`" conditional on guessing a limit nothing tells you; and `llms-full.txt` published HTML comments as visible text, one naming an internal path. **Filed 2026-08-21 as accentcms `b190` and `f328`, released in accent 0.25.0** the same day; all five verified against the released binary by `yqr-f024` §2, per finding. `f328` — a glob to *demote* pages to `## Optional` rather than drop them — stays open upstream and yqr does not need it, since `yqr-f021` moved the spec tree to its own site | `yqr-f021`, `yqr-f010`, `yqr-b007` |
| [b016](yqr-b016-emitted-block-collection-in-a-sequence-keeps-a-trailing-space.md) | The serializer writes trailing whitespace: a dangling indicator in a sequence item, and the indent before an empty block-scalar line | Low | Resolved — the serializer wrote trailing whitespace in two places: a dangling indicator before a block collection reached through a **sequence item**, and the block's indent before an **empty block-scalar line**. Valid YAML that loaded back identically, which is why it was Low; what it broke is the diff, since `git diff --check` and `yamllint` reject it. Pre-existing and reachable through `--normalize` long before `yqr-f017` found it — `to_entries` output is exactly the first shape, so most pairs carried one. Deliberately **not** worked around in yqr: stripping trailing whitespace per rendered line changes a block scalar whose content legitimately ends a line with spaces (`"a␣␣\\nb"` becomes `"a\\nb"`, measured), and silently altering a string is strictly worse than a cosmetic space — so it was pinned as it behaved instead. **Filed 2026-08-21 as noyalib#297, fixed in noyalib#298, released in noyalib 0.0.27** the same day; both faces verified against the published crate by `yqr-f023` §2.1 with the controls byte-identical. The second face was found *while fixing the first*, by holding the patch to a property rather than to the reproduction — 36 corpus documents to the indicator's 9. The pin is what caught the release: the suite came back with exactly one failure, which is the `yqr-m003` rule working as intended | `yqr-f017`, `yqr-b015`, `yqr-a001` |
| [b015](yqr-b015-flow-delete-leaves-a-whitespace-only-line.md) | Deleting a member of a wrapped flow collection leaves a whitespace-only line | Low | Resolved — `del(.ports[0])` on a wrapped flow collection removed the member and its separator but left the line's indentation, so a line that had no trailing whitespace gained two spaces of it. Valid YAML that loads back correctly, which is why it was Low; what it broke is the diff, and `git diff --check`, `yamllint` and most pre-commit setups flag exactly that. Upstream's, not yqr's — the flow class is delegated to `Document::remove` (`yqr-f016` §5) and calling it directly produced the same bytes. Invisible until `yqr-b011` was fixed, for the plainest possible reason: the file could not be parsed. **Filed 2026-08-20 as noyalib#294, fixed in noyalib#296, released in noyalib 0.0.26** the same evening — argued on the self-inconsistency that carried `b010` and `b014`: noyalib's own block path already takes a removed entry's whole line, indentation included, so the flow path leaving an indent behind was the library disagreeing with itself. Two adjacent shapes declined on purpose and said so up front: the last member keeps the comma on the line above (valid, and reaching up a line would be reformatting), and a comment on the member's line keeps the line (a `b010`-class semantics question). Verified against the published crate by `yqr-f020` §3 with all four controls, and the regression test `b015` §5 had deferred is now in `tests/cli.rs` | `yqr-b011`, `yqr-b006`, `yqr-b010`, `yqr-b014`, `yqr-f016`, `yqr-f019`, `yqr-a001` |
| [b014](yqr-b014-empty-collection-at-the-keys-column-is-invalid-yaml.md) | An empty collection written at its key's own column is invalid YAML that noyalib accepts | Medium | Resolved — both faces closed. The **validator** false negative was fixed 2026-08-19: `validate`'s green-tree walk measures each block-mapping entry's value against its key's column and reports `Y103` in default mode, with the two genuinely-valid same-column layouts exempt (a block sequence at its key's column — the GitHub Actions idiom — and a block scalar), every exemption checked against PyYAML and Psych rather than reasoned from the spec text. The **writer** was upstream's: filed 2026-08-19 as noyalib#283 on the argument that upstream's own re-parse guard already refused the identical removal when the mapping had no following sibling — its own code disagreeing with itself, the shape of argument that carried `b010` — fixed in noyalib#284 and **released in noyalib 0.0.25** (2026-08-20). `remove` now indents a sole-entry replacement past its key, matching what `delete_entry` already wrote; verified against the published crate by `yqr-f019` §3.4 with the BOM, CRLF, head-comment and nested variants, and the output accepted by both PyYAML and Psych. noyalib's parser still *accepts* the shape, which is why the `Y103` check stays — the leniency is what made the writer defect invisible. Gated the `yqr-f018` §5 delegation revisit, run in `yqr-f019` §4: zero divergence, and the sole-entry delete stays in `delete_entry` on the standing `yqr-f007` §6 argument alone | `yqr-f018`, `yqr-f016`, `yqr-b006`, `yqr-b010`, `yqr-b011`, `yqr-f012` |
| [b013](yqr-b013-inserted-scalars-take-a-document-wide-quote-style.md) | An inserted scalar is quoted because some unrelated line is quoted | Low | Resolved — noyalib spelled an inserted string with the document's dominant quote style, and the vote counted only quoted scalars against each other, so one `value: "30"` anywhere made every later insertion quoted however plain its neighbours were. Cosmetic and non-destructive, but it landed at the one line a reader is looking at, and it is what `a001` exists to prevent. Found by the `yqr-m003` write tier on its first run and pinned there as it behaved. **Filed 2026-08-19 as noyalib#290** and — unlike the other three — deliberately *without* a patch: a heuristic with a public API attached (`Document::dominant_quote_style` is documented and doctested), so what it counts was the maintainer's call. That call came back in **noyalib 0.0.25** (2026-08-20), taking the second of the two options the filing offered: score the vote at the **edit site** rather than teach the document-wide vote to count plain scalars. An untouched line elsewhere no longer decides an insertion's spelling; a quoted *neighbour* still carries, which is the behaviour the heuristic exists for and is pinned as its own assertion — `yqr-f019` §3.3 | `yqr-a001`, `yqr-b008`, `yqr-f006`, `yqr-m003` |
| [b012](yqr-b012-insert-refused-when-every-key-holds-a-dot.md) | A new key cannot be inserted into a mapping whose keys all hold a `.` | Medium | Resolved — `.metadata.labels.tier = "web"` on a Kubernetes manifest was refused, and the message blamed a `<<` merge the file did not contain. Upstream's `mapping_insert_anchor` composed each candidate anchor key back into a path **string** and re-parsed it, and `parse_query_path` splits on `.` unconditionally, so every key of a dotted mapping was skipped and the only error the function knew about fired. Two defects nested: the `yqr-f007` §6 addressing limit reaching a mapping it was never meant to reach, and a diagnostic asserting a cause instead of describing the observation. Found by the `yqr-m003` write tier on its first run and **filed 2026-08-19 as noyalib#288, fixed in noyalib#289, released in noyalib 0.0.25** (2026-08-20): the anchor comes from the *span tree*, which already holds the target mapping's entries with their spans, so no key is composed back into a path string. Verified by `yqr-f019` §3.2, and the corpus pin flipped from `Err(5)` to a `Rewrites`. The `yqr-f007` §6 limit underneath is **not** closed: `set`, `del`, `key(...)` and the reorder verbs still address through `parse_query_path`, so writing an *existing* dotted key is still refused — now saying so accurately | `yqr-f007`, `yqr-a002`, `yqr-b004`, `yqr-m003` |
| [b011](yqr-b011-multiline-flow-collection-fails-to-parse.md) | A multi-line flow collection is valid YAML that yqr cannot read at all | Medium | Resolved — noyalib refused to parse a flow collection spread over several lines (`ports: [` / `  80,` / `]`), which libyaml, PyYAML and Psych all accept and no yaml-test-suite case forbids; the message named the right indentation rule and applied it to the closing indicator. Loud rather than silent (exit 5, no damage), but it was a **whole-file read** refusal, so `a001`'s guarantee was vacuous for these files and `validate` called them unreadable. Found while reviewing the `yqr-f016` §5 flow-delete work — the "flow deletes only work on single-line collections" limitation was a symptom of this. **Filed 2026-08-19 as noyalib#285, fixed in noyalib#286, released in noyalib 0.0.25** (2026-08-20), yqr's first contribution to noyalib's *parser* rather than its mutators: the more-indented rule for flow continuation exists because under-indented content is ambiguous with sibling block content, and a `]` or `}` cannot begin block content, so the rule was reaching the terminator too. Under-indented *content* stays refused, which is narrower than libyaml — the ecosystem is more lenient than the spec here, and the fix follows the spec. Verified by `yqr-f019` §3.1 and pinned by a `wrapped-flow` fidelity case. Unblocking the read exposed a delete defect behind it, filed as `yqr-b015` | `yqr-a001`, `yqr-b004`, `yqr-f016` |
| [b010](yqr-b010-reorder-moves-values-not-entries.md) | `swap_items` / `move_item` move values, not entries: every comment stays behind | Medium | Resolved — noyalib's reorder mutators exchanged **value bytes** only, so a reorder silently re-attributed every comment in the range at **exit 0**, past upstream's guard by construction (it compares typed values, and a comment is not in one). Filed as noyalib#269 and **corrected in flight**: upstream's behaviour was deliberate, documented and tested (*"the comment annotates the slot"*), so this was a semantics disagreement, not a defect of the b004 §6.1/§6.4 class. yqr's argument — `remove` already decides the same question the other way for the same bytes — was accepted; the fix is yqr's own commit `d397330`, landed via upstream #271 and **released in noyalib 0.0.23**, verified against the published crate by `yqr-f016` §3. Unblocked the `swap`/`move` slice of `yqr-a002` §9, which shipped 2026-08-18 (`yqr-f007` §9) with the §2 measurement as a yqr regression test | `yqr-b004`, `yqr-b006`, `yqr-a002`, `yqr-f007`, `yqr-f015`, `yqr-f016` |
| [b004](yqr-b004-noyalib-cst-mutation-api-gaps.md) | noyalib CST mutation-API gaps: comment editing, key rename, sequence reorder, nested/multi-line delete | Medium | Resolved — all five gaps of umbrella issue noyalib#221 released in **noyalib 0.0.18** (crates.io 2026-07-31): our noyalib#222 (key rename) and noyalib#223 (`Emit` auto-formatting tier), both merged, the other three by the maintainer. Two adoption findings stand (b004 §6): upstream `remove` diverges on delete trivia in **three** measured ways, so delete stays yqr's own code by decision rather than necessity (`yqr-f007` §5.1, would otherwise regress `yqr-b006`); and `key_span` does **not** replace `validate`'s green-tree walk. Those divergences were filed as noyalib#225 and fixed by yqr's noyalib#226 — **merged 2026-08-05, released in noyalib 0.0.19** (§6.4). yqr pins `noyalib = "0.0.22"` as of `yqr-f015`. The maintainer's 2026-08-11 status update on #221 listed three of these gaps as still open; yqr's per-item correction from the published crates (`yqr-f014` §4) was **accepted in the 2026-08-14 update**, which now records comment mutation, `rename_key` and sequence reorder as shipped and re-scopes extended `remove` and fragment quoting as partial (`yqr-f015` §5). Two later adoption findings are recorded in §6.5/§6.6 and do **not** reopen this bug: the reorder trivia semantics are disputed in **`yqr-b010`** (open, above), and upstream's leading-comment mutators absorb a blank-detached comment block — documented upstream behaviour, so no defect, but the opposite of `delete_entry`'s rule, which is why `yqr-a002` §4.1.1 declines to adopt it | `yqr-b002`, `yqr-b006`, `yqr-b008`, `yqr-b010`, `yqr-r002`, `yqr-m002`, `yqr-m005`, `yqr-f006`, `yqr-f007`, `yqr-f013`, `yqr-f014`, `yqr-f015`, `yqr-a002` |
| [b009](yqr-b009-inserted-lines-break-crlf-documents.md) | An inserted line ends with `\n`, giving a CRLF document mixed line endings | Medium | Resolved — noyalib's insertion mutators hard-coded `\n`, so new-key assignment and `+=` gave a CRLF file one bare LF per added line, at **exit 0**. Known upstream behaviour recorded in `b004` §2.5 but never filed, so nothing tracked or tested it. Fixed upstream by yqr's **noyalib#261, merged unmodified 2026-08-14 and released in noyalib 0.0.22**, raised on noyalib#221 (b009 §6); the defect was wider there — the fragment mutators and both comment setters shared it, and `set_comment(Inline)` spliced between the `\r` and the `\n`. yqr carried a local `emit` restore in the meantime (`f014`); `f015` pins 0.0.22 and **deletes** it, since the engine now derives an inserted line's terminator from the document the same way it already derived the indentation. The five byte-exact tests survive unchanged and now pin the engine — with the workaround gone, three of them fail against a temporary 0.0.21 pin. Read, `set_value` and `del` were never affected | `yqr-b001`, `yqr-b004`, `yqr-b008`, `yqr-f014`, `yqr-f015`, `yqr-a001` |
| [b008](yqr-b008-fragment-splice-corrupts-multiline-inserts.md) | Hand-built fragments corrupt `+=` and new-key inserts of a multi-line string | High | Fixed — `+=` produced unparseable output and new-key assignment a wrong value whenever the RHS string contained a newline, both at **exit 0**, so `-i` wrote the damage to the user's file and reported success. `value_fragment` rendered the value to text, and the fragment-taking mutators splice a fragment verbatim — synthesising indentation for its first line only — so a block scalar's continuation lines kept the rendering's indentation instead of the insertion site's. Both paths now use noyalib's typed tier (`insert_entry_value` / `push_back_value`), which owns the indentation and holds the splice to a load-back oracle. Three regression tests assert emitted bytes **and** loaded-back value. Ships with the 0.0.21 pin (`yqr-f014`) | `yqr-f006`, `yqr-f014`, `yqr-b004`, `yqr-f013`, `yqr-f007` |
| [b007](yqr-b007-site-links-broken-under-accent-0-23-1.md) | Website: demo-script media link and 404-page `/docs` link break under accent v0.23.1 | Low | Resolved — demo script now linked on GitHub (accent refuses `.sh` as media), 404 page links `/specs/`; CI pins accent v0.23.1 and builds with `--strict-links` so missing link targets fail the build | `yqr-f010` |
| [b001](yqr-b001-roundtrip-discards-whitespace-and-formatting.md) | Round-trip through the default pipeline discards whitespace, comments, and formatting | High | Resolved for the default read — byte fidelity is now the **default** (`yqr-f009`); `yqr '.' f` is byte-for-byte identical to `f` via the fidelity engine (`yqr-f002`). The lossy semantic round trip is now opt-in via `--normalize` (by design) | `yqr-a001`, `yqr-r001`, `yqr.f001`, `yqr-f002`, `yqr-f009`, `yqr-m005` |
| [b006](yqr-b006-structural-delete-trivia-and-fidelity.md) | Structural delete mishandles comments, blank lines, and same-column sequences | High | Resolved — `owned_line_span` now derives its range from noyalib's value span, commits via byte-preserving `replace_span`, and folds a same-indent head comment into the delete; regression tests cover each case | `yqr-f007`, `yqr-b004`, `yqr-a001` |
| [b005](yqr-b005-crossbeam-epoch-advisory-via-criterion.md) | `cargo audit` fails on `crossbeam-epoch` advisory (RUSTSEC-2026-0204) via `criterion` dev-dep | Low | Resolved — `cargo update -p crossbeam-epoch` bumped the transitive pin `0.9.18 -> 0.9.20`; `cargo audit` exits 0, lockfile-only change | `yqr-m001` |
| [b002](yqr-b002-noyalib-cst-span-and-key-model-deficiencies.md) | noyalib CST deficiencies: span boundaries, duplicate-key policy, string-only key model | Medium | Resolved — all 7 fixes released upstream in noyalib 0.0.14 (2.1 in 0.0.13; 2.2-2.7 in the v0.0.14 release #160); yqr pins crates.io `noyalib = "0.0.14"`, git-dep dropped — `yqr-m004`, `yqr-f004` | `yqr-f002`, `yqr-f004`, `yqr-r002`, `yqr-m002` |
| [b003](yqr-b003-rustyaml-roundtrip-trailing-doc-end-marker.md) | rust-yaml fork `RoundTripDocument::parse_all` errors on a trailing `...` after a block collection | Medium | Resolved (moot) — the rust-yaml fork backend was removed when yqr consolidated on noyalib (`yqr-m005`); no longer affects yqr | `yqr-m005`, `yqr-b001`, `yqr-m002` |

## Summary

- Total bugs: 23
- Open: **2** (b021, b022) — both **fixed upstream and awaiting a noyalib
  release**, so neither is work yqr holds. `b022` is the more serious: it makes
  `validate` report an error on valid YAML.
- **Every bug yqr has filed is fixed**, in yqr or in a released dependency,
  except `b021` and `b022`, both of which have a merged-or-open fix upstream.
  Four of the last five were found by the work that closed the one before:
  `b020` by `b019`'s documentation pass, `b021` by `b020`'s boundary test,
  `b022` by a test `b021`'s upstream patch could not write, and `b023` by
  smoke-testing `0.7.0` from crates.io instead of from the build directory.
- **Every bug yqr filed against accent is fixed in a released version** —
  `b007` in accent 0.23.1, `b017`'s five findings in 0.25.0, filed and
  released the same day (`yqr-f024`). What is still open upstream is accentcms
  `f328`, a feature request yqr filed and does not need.
- **Every bug yqr filed against noyalib is now fixed in a published release**
  — `b011`–`b014` in 0.0.25, `b015` in 0.0.26, `b016` in 0.0.27. Six bugs,
  three releases, four days; five of the six fixed by yqr's own commits
  upstream (`yqr-f023` §4).
- **noyalib 0.0.26 (2026-08-20) closed `b015`** — one functional change,
  yqr's noyalib#296, released hours after it merged. Verified against the
  published crate on its own reproduction with all four controls
  (`yqr-f020` §3).
- Resolved: 21 (b023, b020, b019, b018, b017, b016, b015 — see above; b014, b013, b012, b011 —
  closed by noyalib 0.0.25, `yqr-f019`;
  b010 — noyalib's reorder exchanged value bytes only, so every
  comment stayed with the slot; filed as noyalib#269, re-framed from "defect"
  to a semantics disagreement once its pinning test turned up, argued on
  `remove`/reorder inconsistency, and fixed by yqr's own commit in 0.0.23;
  b009 — an inserted line ended with `\n` regardless of the
  document's convention, silently giving CRLF files mixed endings; worked
  around in `emit` (`yqr-f014`), then fixed at the source by yqr's
  noyalib#261, released in 0.0.22, and the workaround deleted (`yqr-f015`);
  b008 — `+=` and new-key assignment of a multi-line string
  corrupted the document at exit 0; both insert paths now route through
  noyalib's typed insertion tier, `yqr-f014`;
  b004 — all five mutation-API gaps released in noyalib
  0.0.18 and adopted by `yqr-f013`'s pin bump; delete deliberately keeps
  yqr's own path, since upstream `remove` diverges on entry trivia in
  four measured cases — those divergences are now fixed upstream by yqr's
  noyalib#226, released in 0.0.19 and pinned by `yqr-f014`;
  b007 — website links fixed for the accent v0.23.1 media
  policy and link checker, CI now builds with `--strict-links`;
  b001 — byte fidelity is now the default read (`yqr-f009`), closing
  the lossy-default round trip; the classic pipeline is now opt-in via
  `--normalize`;
  b006 — structural-delete trivia/fidelity defects fixed via a
  span-derived range + byte-preserving `replace_span`, `yqr-f007`;
  b005 — `crossbeam-epoch` advisory cleared by `cargo update`;
  b002 — 2.2-2.7 fixes released in noyalib 0.0.14, git-dep dropped;
  b003 — moot after the single-engine consolidation removed the rust-yaml
  backend, `yqr-m005`)
