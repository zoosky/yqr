# yqr

> yqr is a jq-style query and editing tool for YAML, written in Rust. Reads are byte-preserving by default: nodes you do not touch are emitted as their original bytes, so comments, quoting, indentation and line endings survive. The write tier changes only the bytes the filter targets, or refuses.

Install: cargo install yqr
Invoke:  yqr [-r] [-i] [-N] '<filter>' [file]      (stdin when file is omitted)
         yqr validate [--strict] <file>...

Flags:
  -r, --raw-output   print a string's value, not its YAML quoting
  -i, --in-place     write the edit back to the file (mutating filters only)
  -N, --normalize    re-serialize instead of preserving bytes

Exit codes:
  0  success
  1  validate found a problem in the file
  3  the filter could not be lexed or parsed
  5  runtime or I/O error, including a refused edit

Source:    https://github.com/zoosky/yqr
Package:   https://crates.io/crates/yqr

## How yqr compares to other YAML tools

- [How yqr compares to other YAML tools](/yqr/compare): There are good YAML tools already. These pages are about where yqr differs from them, measured rather than asserted -- …
- [yqr and yq -- what actually changes in your file](/yqr/compare/yq): Let's get the important part out of the way first: yq is a good tool. It is mature, it does far more than yqr does, and…

## yqr Demo

- [yqr Demo](/yqr/demo): A runnable showcase of yqr, a jq-style query & transform tool for YAML.

## yqr guide

- [Byte-for-byte YAML editing, explained](/yqr/guide/fidelity): Most YAML tools work by parsing your file into data, doing something to the data, and printing the data back out. That …
- [Editing Kubernetes manifests without reformatting them](/yqr/guide/kubernetes): Manifests are checked in, reviewed, and argued over. So the useful property in a tool that edits them is not how much i…
- [Enumerating a mapping without losing the keys](/yqr/guide/enumerate): The most ordinary shape in YAML is a mapping of named things -- services, environments, jobs, hosts. Iterating one give…
- [Validating YAML from the command line](/yqr/guide/validate): An edit went in -- by hand, by a script, or by an agent. Is the file still correct?
- [yqr guide](/yqr/guide): Short, task-shaped pages. Every command in them was run against a real build, and the output you see is the output it p…

## Optional

- [RSS Feed](/yqr/feed.xml): Subscribe to content updates
- [Sitemap](/yqr/sitemap.xml): Full site map for crawlers

