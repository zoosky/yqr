/target
**/*.rs.bk
Cargo.lock.orig
*.pdb

# Per-machine agent settings: local paths/secrets, never commit
.agent/settings.local.json

# Accent CMS website build artifacts (docs/ is the site root)
docs/output/
# The internal docs+specs site (specs/config.yaml, port 4401) builds here.
specs/output/
docs/.build-manifest.json
docs/.media-cache/
docs/.diagram-cache/
docs/.remote-cache/

# Browser-automation scratch output (playwright MCP snapshots/screenshots).
.playwright-mcp/

# Reference testbed (yqr-m007): the js-yaml install and the go build cache.
# `bun.lock` and `package.json` are committed so the pin is reproducible.
testbed/node_modules/
