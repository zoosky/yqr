# Reference testbed: recorded answers

Generated by `python3 testbed/run.py`. Do not edit by hand.

Each row is one implementation's answer to one question about one
document. A refusal is an answer, and so is `unsupported`: it is why a
comment question cannot be settled by citing PyYAML.

| implementation | version |
|---|---|
| PyYAML | PyYAML 6.0.3 |
| Psych | Psych 3.1.0 (libyaml 0.2.1) |
| js-yaml | js-yaml 4.1.0 |
| go-yaml | go-yaml v3.0.1 |
| ruamel | ruamel.yaml 0.19.1 |

## implicit-null-inline-comment

noyalib#425 and yqr-b031. Does a comment after a colon with no value belong to the entry? noyalib's comment API says there is nothing there; its own `remove` says the comment is the entry's.

```yaml
k:   # todo
after: 1  # kept

```

Path: `["k"]`

### load

| implementation | answer |
|---|---|
| PyYAML | `{"k": null, "after": 1}` |
| Psych | `{"k":null,"after":1}` |
| js-yaml | `{"k":null,"after":1}` |
| go-yaml | `{"after":1,"k":null}` |
| ruamel | `{"k": null, "after": 1}` |

### roundtrip

| implementation | answer |
|---|---|
| PyYAML | `after: 1\nk: null\n` |
| Psych | `---\nk: \nafter: 1\n` |
| js-yaml | `k: null\nafter: 1\n` |
| go-yaml | `k: # todo\nafter: 1 # kept\n` |
| ruamel | `k:   # todo\nafter: 1  # kept\n` |

### comments

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `{"key_line":["todo"]}` |
| ruamel | `{"post_value": ["# todo"]}` |

### delete

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `after: 1 # kept\n` |
| ruamel | `after: 1  # kept\n` |

### set_comment

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `k: # set\nafter: 1 # kept\n` |
| ruamel | `k:  # set\nafter: 1  # kept\n` |

## implicit-null-head-comment

The other half of the same question, and the one where implementations are known to disagree about deletion.

```yaml
# doc k
k:
after: 1

```

Path: `["k"]`

### load

| implementation | answer |
|---|---|
| PyYAML | `{"k": null, "after": 1}` |
| Psych | `{"k":null,"after":1}` |
| js-yaml | `{"k":null,"after":1}` |
| go-yaml | `{"after":1,"k":null}` |
| ruamel | `{"k": null, "after": 1}` |

### comments

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `{"key_head":["doc k"]}` |
| ruamel | `{}` |

### delete

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `after: 1\n` |
| ruamel | `# doc k\nafter: 1\n` |

## implicit-null-comment-at-end-of-input

The `yqr-b022` layout crossed with the comment question: the entry has no value and the file has no final newline, so there is nothing after the comment to anchor on either.

```yaml
a: 1
k:   # todo
```

Path: `["k"]`

### load

| implementation | answer |
|---|---|
| PyYAML | `{"a": 1, "k": null}` |
| Psych | `{"a":1,"k":null}` |
| js-yaml | `{"a":1,"k":null}` |
| go-yaml | `{"a":1,"k":null}` |
| ruamel | `{"a": 1, "k": null}` |

### comments

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `{"key_line":["todo"]}` |
| ruamel | `{"post_value": ["# todo"]}` |

### set_comment

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `a: 1\nk: # set\n` |
| ruamel | `a: 1\nk:  # set\n` |

## empty-sequence-item-with-a-comment

The sequence half. A `-` with nothing after it carries a comment the same way a key does, and the entry has no key token to hang it on.

```yaml
xs:
  -   # todo
  - 1

```

Path: `["xs", 0]`

### load

| implementation | answer |
|---|---|
| PyYAML | `{"xs": [null, 1]}` |
| Psych | `{"xs":[null,1]}` |
| js-yaml | `{"xs":[null,1]}` |
| go-yaml | `{"xs":[null,1]}` |
| ruamel | `{"xs": [null, 1]}` |

### comments

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `{}` |
| ruamel | `{}` |

### delete

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `xs:\n    # todo\n    - 1\n` |
| ruamel | `xs:\n      # todo\n- 1\n` |

### set_comment

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `xs:\n    - # set\n    # todo\n    - 1\n` |
| ruamel | `xs:\n-  # set\n      # todo\n- 1\n` |

## implicit-null-nested

Whether the answer depends on depth, which it should not.

```yaml
outer:
  k:   # todo
  after: 1

```

Path: `["outer", "k"]`

### comments

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `{"key_line":["todo"]}` |
| ruamel | `{"post_value": ["# todo"]}` |

### set_comment

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `outer:\n    k: # set\n    after: 1\n` |
| ruamel | `outer:\n  k:  # set\n  after: 1\n` |

## block-value-at-its-keys-column

yqr-b014 and the Y103 check. noyalib reads this; the question is whether anyone else does.

```yaml
on:
[]
jobs: {}

```

### load

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning a simple key` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): could not find expected ':' while scanning a simple key at line 2 column 1` |
| js-yaml | **refused** `YAMLException: can not read an implicit mapping pair; a colon is missed (2:3)` |
| go-yaml | **refused** `*errors.errorString: yaml: line 2: could not find expected ':'` |
| ruamel | **refused** `ScannerError: while scanning a simple key` |

## scalar-at-its-keys-column

yqr-b029. What `set_value` used to emit when a scalar replaced a block collection. yqr refuses to produce it; this records who would read it.

```yaml
k:
5
after: 1

```

### load

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning a simple key` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): could not find expected ':' while scanning a simple key at line 2 column 1` |
| js-yaml | **refused** `YAMLException: can not read a block mapping entry; a multiline key may not be an implicit key (3:6)` |
| go-yaml | **refused** `*errors.errorString: yaml: line 2: could not find expected ':'` |
| ruamel | **refused** `ScannerError: while scanning a simple key` |

## colon-at-end-of-input

yqr-b022. `a:` and `a:\n` are one byte apart and must mean the same thing.

```yaml
a:
```

### load

| implementation | answer |
|---|---|
| PyYAML | `{"a": null}` |
| Psych | `{"a":null}` |
| js-yaml | `{"a":null}` |
| go-yaml | `{"a":null}` |
| ruamel | `{"a": null}` |

### roundtrip

| implementation | answer |
|---|---|
| PyYAML | `a: null\n` |
| Psych | `---\na: \n` |
| js-yaml | `a: null\n` |
| go-yaml | `a:\n` |
| ruamel | `a:\n` |

## empty-sequence-item

yqr-b031. A `-` with nothing after it, and what removing it leaves.

```yaml
xs:
  - 1
  -

```

Path: `["xs", 1]`

### load

| implementation | answer |
|---|---|
| PyYAML | `{"xs": [1, null]}` |
| Psych | `{"xs":[1,null]}` |
| js-yaml | `{"xs":[1,null]}` |
| go-yaml | `{"xs":[1,null]}` |
| ruamel | `{"xs": [1, null]}` |

### delete

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `xs:\n    - 1\n` |
| ruamel | `xs:\n- 1\n` |

## sole-entry-of-a-block

yqr-f016 §5. Deleting the only entry cannot leave a dangling `spec:`, which would re-parse as null. What each implementation writes instead is the design question.

```yaml
spec:
  replicas: 3
kind: Deployment

```

Path: `["spec", "replicas"]`

### load

| implementation | answer |
|---|---|
| PyYAML | `{"spec": {"replicas": 3}, "kind": "Deployment"}` |
| Psych | `{"spec":{"replicas":3},"kind":"Deployment"}` |
| js-yaml | `{"spec":{"replicas":3},"kind":"Deployment"}` |
| go-yaml | `{"kind":"Deployment","spec":{"replicas":3}}` |
| ruamel | `{"spec": {"replicas": 3}, "kind": "Deployment"}` |

### delete

| implementation | answer |
|---|---|
| PyYAML | _unsupported: PyYAML keeps no comments and has no editing model_ |
| Psych | _unsupported: Psych keeps no comments and has no editing model_ |
| js-yaml | _unsupported: js-yaml keeps no comments and has no editing model_ |
| go-yaml | `spec: {}\nkind: Deployment\n` |
| ruamel | `spec: {}\nkind: Deployment\n` |

## octal-and-version-scalars

yqr-b018 and the fidelity guide's two headline examples. `0640` must not become 640, `1.10` must not become 1.1.

```yaml
mode: 0640
ver: 1.10
zip: 007

```

### load

| implementation | answer |
|---|---|
| PyYAML | `{"mode": 416, "ver": 1.1, "zip": 7}` |
| Psych | `{"mode":416,"ver":1.1,"zip":7}` |
| js-yaml | `{"mode":640,"ver":1.1,"zip":7}` |
| go-yaml | `{"mode":416,"ver":1.1,"zip":7}` |
| ruamel | `{"mode": 640, "ver": 1.1, "zip": 7}` |

### roundtrip

| implementation | answer |
|---|---|
| PyYAML | `mode: 416\nver: 1.1\nzip: 7\n` |
| Psych | `---\nmode: 416\nver: 1.1\nzip: 7\n` |
| js-yaml | `mode: 640\nver: 1.1\nzip: 7\n` |
| go-yaml | `mode: 0640\nver: 1.10\nzip: 007\n` |
| ruamel | `mode: 0640\nver: 1.10\nzip: 007\n` |

## merge-key

yqr-b020. Whether `<<` is resolved on load, and into what.

```yaml
d: &d
  a: 1
u:
  <<: *d
  b: 2

```

### load

| implementation | answer |
|---|---|
| PyYAML | `{"d": {"a": 1}, "u": {"a": 1, "b": 2}}` |
| Psych | `{"d":{"a":1},"u":{"a":1,"b":2}}` |
| js-yaml | `{"d":{"a":1},"u":{"a":1,"b":2}}` |
| go-yaml | `{"d":{"a":1},"u":{"a":1,"b":2}}` |
| ruamel | `{"d": {"a": 1}, "u": {"b": 2, "a": 1}}` |

## duplicate-key

yqr-f012's Y101. Every implementation accepts it; the question is which wins.

```yaml
a: 1
b: 2
a: 3

```

### load

| implementation | answer |
|---|---|
| PyYAML | `{"a": 3, "b": 2}` |
| Psych | `{"a":3,"b":2}` |
| js-yaml | **refused** `YAMLException: duplicated mapping key (3:1)` |
| go-yaml | **refused** `yaml: unmarshal errors:\n  line 3: mapping key "a" already defined at line 1` |
| ruamel | **refused** `DuplicateKeyError: while constructing a mapping` |

## crlf-document

yqr-b009 and yqr-b030. Whether a round trip keeps the document's terminators.

```yaml
a: 1\r\n ->
b: 2\r\n ->

```

### load

| implementation | answer |
|---|---|
| PyYAML | `{"a": 1, "b": 2}` |
| Psych | `{"a":1,"b":2}` |
| js-yaml | `{"a":1,"b":2}` |
| go-yaml | `{"a":1,"b":2}` |
| ruamel | `{"a": 1, "b": 2}` |

### roundtrip

| implementation | answer |
|---|---|
| PyYAML | `a: 1\nb: 2\n` |
| Psych | `---\na: 1\nb: 2\n` |
| js-yaml | `a: 1\nb: 2\n` |
| go-yaml | `a: 1\nb: 2\n` |
| ruamel | `a: 1\nb: 2\n` |

## tab-before-a-sequence-dash

noyalib 0.0.36 started refusing this on the first line, having read it before. yqr passes the parser's answer through, so it is worth knowing whose answer that matches.

```yaml
	- a

```

### load

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning for the next token` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): found character that cannot start any token while scanning for the next token at line 1 column 1` |
| js-yaml | **refused** `YAMLException: end of the stream or a document separator is expected (1:2)` |
| go-yaml | **refused** `*errors.errorString: yaml: found character that cannot start any token` |
| ruamel | **refused** `ScannerError: while scanning for the next token` |

## tab-indented-comment-after-a-plain-scalar

Found while validating the noyalib#418 fix. A comment line indented with a leading tab, after a plain scalar. noyalib accepts it; the question is whether anyone else does.

```yaml
k: 1
	# t

```

Path: `["k"]`

### load

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning for the next token` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): found a tab character that violate indentation while scanning a plain scalar at line 1 column 4` |
| js-yaml | `{"k":1}` |
| go-yaml | **refused** `*errors.errorString: yaml: line 2: found a tab character that violates indentation` |
| ruamel | **refused** `ScannerError: while scanning for the next token` |

### roundtrip

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning for the next token` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): found a tab character that violate indentation while scanning a plain scalar at line 1 column 4` |
| js-yaml | `k: 1\n` |
| go-yaml | **refused** `*errors.errorString: yaml: line 2: found a tab character that violates indentation` |
| ruamel | **refused** `ScannerError: while scanning for the next token` |

## tab-indented-comment-after-a-quoted-scalar

The same line after a quoted scalar. noyalib rejects this one, so its answer depends on the quote style of the line above, which no rule about tabs should. Whichever way it settles, one of the two is wrong.

```yaml
k: "1"
	# t

```

Path: `["k"]`

### load

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning for the next token` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): found character that cannot start any token while scanning for the next token at line 2 column 1` |
| js-yaml | `{"k":"1"}` |
| go-yaml | **refused** `*errors.errorString: yaml: line 2: found character that cannot start any token` |
| ruamel | **refused** `ScannerError: while scanning for the next token` |

### roundtrip

| implementation | answer |
|---|---|
| PyYAML | **refused** `ScannerError: while scanning for the next token` |
| Psych | **refused** `Psych::SyntaxError: (<unknown>): found character that cannot start any token while scanning for the next token at line 2 column 1` |
| js-yaml | `k: '1'\n` |
| go-yaml | **refused** `*errors.errorString: yaml: line 2: found character that cannot start any token` |
| ruamel | **refused** `ScannerError: while scanning for the next token` |

